告诉计算机对值或变量执行哪些操作的符号或关键字叫运算符

一、运算符类型
运算符有很多种类型，但最常见的有：

算术运算符（+、- 等）
赋值运算符（=、+=、-= 等）
比较运算符（==、>、< 等）
逻辑运算符（&&、|| 等）
位运算符（&、| 等）

二、位运算符
最常用的位运算符有：

&（按位与运算）
|（按位或）
^（按位异或）
~（按位取反）
<<（左移）
>>（右移）
例
a = 0b1010
b = 0b0110
result = a | b
print(bin(result))
使用按位或运算符|来组合以二进制格式存储的两个变量
