一、类型转换（或类型强制转换）是将一个值从一种数据类型更改为另一种数据类型的过程。
当你想在计算中使用某个值，但该值存储为另一种类型（例如文本字符串）时，这非常有用。

例如，如果我们有一个字符串"5"和一个数字10，我们不能直接将它们相加。首先我们需要将字符串转换为数字：
a = "5"
b = 10
c = int(a) + b    //int整数转换器这里做了转换
print(c)

二、转换有两种类型
1.隐式类型转换（也称为类型提升）——由编程语言自动完成。
显式类型转换——                程序员手动转换类型。
例如，在许多编程语言中，整数可以根据需要自动转换为浮点数：
x = 5      # int
y = 2.0    # float
z = x + y  # int automatically becomes float整数自动变浮点
print(z)

2.显式类型转换不同的语言用不同的方式表达这一点：

Python和JavaScript使用类似float()或的函数Number()。
Java和C++经常在值前面使用带类型的括号，例如(double) x：
x = 5
y = float(x)  # explicit casting with function
print(y)

三、数据类型
1.数据类型是指  可以存储在  变量中的数据类型   例如它是文本还是数字？
2.给变量设置的数据类型会影响我们可以对该变量执行的操作。

3.两个数值数据类型的变量，它们的值分别为 3和 4，我们可以使用+加法运算符将它们相加，得到7
4.把这两个值存储为 文本字符串 数据类型，分别记为 `a` 和"3"`b` 和"4"，那么当我们使用 `+` 运算符时，就会得到"34"，加起到了合并字符串的效果

5.最常见的数据类型有：
字符串（文本）
整数（正整数）
浮点数（十进制数）
布尔值（真或假）
注意：在 Python 和 JavaScript 中，创建变量时不需要指定数据类型，因为数据类型 是自动确定的；
但在 Java 和 C++ 中，创建变量时需要  指定数据类型。

6.查找变量的数据类型
如果你有一个变量，并且想知道它的数据类型，大多数编程语言都有一个 内置函数type()可以实现
例
将值存储3在名为 的变量中x，并检查它是哪种类型的数据
x = 3
print(type(x))              #Python
const x = 3;
console.log(typeof x);     //JavaScript
public class Main {
  public static void main(String[] args) {
    int x = 3;
    System.out.println(((Object)x).getClass().getName());
  }
}                         //Java

7.字符串数据类型
一、字符串是一串字符。
在 Python 和 JavaScript 中，字符串可以  用双引号"Hello"或单引号括起来'Hello'。
但在 Java 和 C++ 中，字符串必须  用双引号括起来"Hello"，并且它们有单独的 字符数据类型，其中使用单引号'H'。
message = 'Hello, World!'
print(message)
对于字符串，+运算符用于连接两个字符串，将它们合并成一个字符串。

二、字符串长度
可以使用内置函数或属性len()来查找字符串中的字符数
txt = "Hello"
print(len(txt))

三、访问字符
字符串就像字符数组，因此您可以使用索引[]（从 0 开始）访问单个字符。注意索引的写法变量txt+[索引位]
txt = "Hello"
print(txt[1])   # prints 'e'

四、常用字符串操作(字符串函数)              这些函数其实就是各种工具，器
最常对字符串进行的操作包括：
转换为大写或小写              txt.upper()   txt.lower()
检查字符串是否包含另一个字符串 "World" in txt在变量txt里包不包含world，返回真或假
替换字符串中的文本            txt.replace("World", "Python")把wor替换为py
txt = "Hello World"
print(txt.upper())        # HELLO WORLD
print(txt.lower())        # hello world
print("World" in txt)     # True
print(txt.replace("World", "Python"))



8.无，或空值
我们给一个变量  赋值为None或null，表示它没有值。
这就是给变量赋值的方法None，或者null：
a = None
print(a)
print(type(a))









