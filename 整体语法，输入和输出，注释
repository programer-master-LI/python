一、整体语法
1.执行 Python 语法
正如我们在上一页中所了解到的，Python 语法可以/直接通过在命令行中输入/来执行：
>>> print("Hello, World!")
Hello, World!
也可以在服务器上创建一个/ Python 文件/，使用 .py 文件扩展名，然后在/命令行中运行/它：
C:\Users\Your Name>python myfile.py        python运行文件

2.Python缩进,缩进是指代码行开头的空格。
在其他编程语言中，//代码缩进/仅仅是为了提高可读性，但在 Python 中，缩进却非常重要。
  ///Python 使用缩进来表示代码块。   
if 5 > 2:
  print("Five is greater than two!")
如果省略缩进，Python 会报错：
语法错误：                表示函数体，是固定格式，代码缩进规范
if 5 > 2:
print("Five is greater than two!")
///空格的数量 由程序员自行决定，最常见的用法是/四个空格//，但至少要有一个空格。
if 5 > 2:
 print("Five is greater than two!") 
if 5 > 2:
        print("Five is greater than two!") 
  ////在同一代码块中，空格的数量必须相同///，否则Python会报错：   （相当重要）
语法错误：
if 5 > 2:
 print("Five is greater than two!")    同一代码块，缩进不一致
        print("Five is greater than two!")

3.变量            x,y,xy,z
在Python中，当你 ////给变量赋值时，变量就被创建了///：
Python中的变量：
x = 5
y = "Hello, World!"
Python 没有声明变量的命令。          (声明变量的命令比如在c语言中声明变量int a 整数变量a)
你将在“变量”章节中了解更多关于变量的知识。

4.注释
Python 具有注释功能，可用于在代码中添加文档。
注释以逗号 ( #&) 开头，Python 会将该行的其余部分渲染成注释：
Python 中的注释：
#This is a comment.
print("Hello, World!")

5.声明
计算机程序是 一系列由计算机“执行”的 “指令”。这些编程 指令被称为语句。
以下语句会在屏幕上打印文本“Python is fun!”：
print("Python is fun!")            不加分号
在Python中，语句通常在行尾结束。你不需要像许多其他编程语言（例如Java或C ）那样使用分号; 。

许多声明
大多数Python程序都包含 许多声明。这些语句会按照 编写顺序 逐一执行：
print("Hello World!")
print("Have a good day.")
print("Learning Python is fun!")
首先执行第一条语句（打印“Hello World!”）。
然后执行第二条语句（打印“Have a good day.”）。
最后执行第三条语句（打印“Learning Python is fun!”）。

分号（可选，很少使用）           （两条语句放在同一行必须有分隔符）
在 Python 中，分号是可选的。你可以用//分号分隔多个语句，将它们写在一行中//，;但这很少使用，因为它会使代码难以阅读：
print("Hello"); print("How are you?"); print("Bye bye!")
但是，如果将///两条语句放在同一行而没有分隔符（换行符或逗号;）//，Python 将会报错：
print("Python is fun!") print("Really!")
结果：
SyntaxError: invalid syntax
最佳实践：将每条语句放在单独的一行，这样你的代码就更容易理解。

二、打印输出文本print()
1.可以使用该print()函数来 显示文本或输出值：
print("Hello World!")
可以根据需要多次使用print()该函数。默认情况下，每次调用都会在 新行上打印文本：
print("Hello World!")
print("I am learning Python.")
print("It is awesome!")

2.双引号
在 Python 中，文本必须用 引号括起来。您可以使用"双引号或'单引号：
print("This will work!")
print('This will also work!')
如果你忘记将文本放在引号内，Python 将会报错：

3.打印多段文本时不换行
默认情况下，该print()函数以换行符结束。
如果要在同一行打印多个单词，可以使用end以下参数：
print("Hello World!", end=" ")            （逗号end=" "）
print("I will print on the same line.")
end=" "为了提高可读性，我们在后面加了一个空格。

4.输出数字打印号码            数字不加引号
您还可以使用print()函数 显示数字：
与文本不同，我们不会 把数字放在引号里：
print(3)
print(358)
print(50000)
你也可以在print()函数 内部进行 数学运算：
print(3 + 3)
print(2 * 5)
混合文字和数字
您可以通过用 逗号分隔，将 文本和数字合并 到一个输出中：
print("I am", 35, "years old.")
在变量和字符串中，您将学习更多关于如何将 文本和变量结合 起来的知识。

三、输入函数input()
1.Python允许用户输入。这意味着我们可以 向用户征求意见。
以下示例会要求您输入姓名，当您输入姓名后，姓名会显示在屏幕上：
征求用户意见：
print("Enter your name:")
name = input()    把input()赋值给name   这里已经完成输入了，///输入完需要按回车才执行后续操作//
print(f"Hello {name}")               打印文本
Python 在执行到input()函数时会停止，并在 用户给出一些输入后 继续执行。

2.输入的提示词
在上面的例子中，用户需要在新的一行输入自己的姓名。
input()函数有一个prompt提示词参数，可以作为一条消息，放在用户 输入内容的前面，///与用户 输入内容显示在同一行///：
在用户输入框前添加一条消息：
name = input("Enter your name:")         name=input(  提示词  )
print(f"Hello {name}")                   ///输入完需要按回车才执行后续操作//

3.多个输入
您可以添加//任意数量的输入框//，Python 会在///每个输入框处暂停执行，等待用户输入///：
多种输入方式：
name = input("Enter your name:")
print(f"Hello {name}")
fav1 = input("What is your favorite animal:")
fav2 = input("What is your favorite color:")
fav3 = input("What is your favorite number:")
print(f"Do you want a {fav2} {fav1} with {fav3} legs?")

4.输入数字
//用户 输入的内容 会被视为字符串//。即使在上面的例子中，你输入的是一个数字，Python 解释器  仍然会将其视为字符串。
您可以使用以下函数将 //输入转换为数字float()// ：
要求平方根，必须将 输入值转换为数字：
x = input("Enter a number:")
#find the square root of the number:
y = math.sqrt(float(x))         float(x)输入值转换为数字
print(f"The square root of {x} is {y}")

5.验证输入
/验证用户输入的任何内容/都是良好的做法。在上面的例子中，如果用户输入的不是数字，就会发生错误。
//为避免出错，我们可以测试输入/，/如果输入的不是数字，则向用户  显示“输入错误，请重试”之类的消息//，并允许用户重新输入：
一直问直到得到号码为止：
y = True                                     等号=是赋值运算
while y == True:               双等号==是比较运算符，判断左右两边是否相等，返回布尔值true,flase
  x = input("Enter a number:")
  try:
    x = float(x);
    y = False
  except:
    print("Wrong input, please try again.")
print("Thank you!")

四、注释     #  """ """
注释可以用来解释Python代码。注释可以使代码 更易读。
在测试代​​码时，可以使用  注释来阻止代码执行。
1.创建注释
注释以#开头，Python 会忽略它们：
#This is a comment
print("Hello, World!")
2.注释可以放在行尾，Python 会忽略该行的其余部分：
print("Hello, World!") #This is a comment
注释不一定是解释代码的文本，它也可以用来 阻止 Python 执行某些代码：
#print("Hello, World!")
print("Cheers, Mate!")
3.多行注释
Python 实际上并 没有支持多行注释的语法。
要添加多行注释，您可以为每一行插入一个 #：
#This is a comment
#written in
#more than just one line
print("Hello, World!")
或者，虽然不太符合预期，但您可以使用 多行字符串。
由于 Python 会忽略//未赋值给变量的字符串//，因此你可以在代码中添加 多行字符串（三引号），并将注释放在其中：
"""
This is a comment
written in
more than just one line
"""
print("Hello, World!")
只要字符串 没有被赋值给变量，Python 就会读取字符串代码，但随后会忽略它，这样你就创建了一个多行注释。











